# ¡A VENDER! Plugin — Default Configuration
# Settings are managed via the Agent Zero Plugin Settings UI.
# NEVER commit real credentials, PINs, or tokens here.

# Admin Backdoor (set via Plugin Settings UI — never hardcode)
# The admin_pin is set by the tenant during first login and stored encrypted in config.json
admin_pin: ""
